const config = {
  theme: {
    default: 'light',
    availableThemes: ['light', 'dark', 'neon']
  },
  modules: {
    header: 'header-base',
    projects: 'projects'
  },
  systems: {
    themeSystem: true,
    renderSystem: false,
    routingSystem: false
  }
};

export default config;
